requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "astro_utils"
version = "0.0.1"

dependencies = [
 "numpy",
 "pandas",
 "matplotlib",
 "astropy",
 "reproject",
 "scipy",
 "scikit-image"
]
